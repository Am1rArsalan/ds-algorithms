// insert into the heap ( function api )
export function insertIntoHeap(value: number) {
    //
}

// heapify up valuse in the heap ( function api )
export function heapifyUp(value: number) {
    //
}

// heapify down valuse in the heap ( function api )
export function heapifyDown(value: number) {
    //
}
