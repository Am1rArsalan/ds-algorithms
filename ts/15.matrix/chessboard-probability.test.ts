import { test, assert } from 'vitest';
import { countIslands } from './';

test("calculate probability of knight's movemnet in chessboard", () => {
    assert.equal(calculateProbability(6, 2), -100);
});
